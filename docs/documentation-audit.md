---
layout: default
title: "Documentation Audit"
nav_exclude: true
search_exclude: true
permalink: /documentation-audit/
description: "Front-matter, navigation, and SEO audit of the StreetJS documentation site."
---

# Documentation Audit

> Generated by `node scripts/audit/site-audit.mjs`. Re-run to refresh.

## Front-matter health

| Check | Result |
|-------|-------:|
| Pages with front-matter & a `title` | **all pass** (0 missing) |
| Pages with front-matter & a `description` | **all pass** (0 missing) |
| Markdown files with **no** front-matter | 11 |

## Files without front-matter (intentional vs. to-fix)

These render via the site-wide default layout (`_config.yml` `defaults`), but have
no nav entry, SEO `title`, or `description`.

**Intentional (deep reference reports — direct-link only):**
`STREETJS-FULL-REPORT.md`, `STREETJS-GAP-ANALYSIS.md`,
`STREETJS-READINESS-ASSESSMENT.md`, `RUNTIME-STABILITY-VERIFICATION.md`,
`PRODUCTION-HARDENING-PROGRAM.md`, `PLATFORM-LEADERSHIP-ADOPTION-PROGRAM.md`,
`ADOPTION-ENTERPRISE-GAP-CLOSURE.md`, `WORKFLOW-AUDIT.md`.
*(`RELEASE_CHECKLIST.md`, `ROLLBACK.md` are excluded from the build in `_config.yml`.)*

**Recommended to add front-matter (user-facing, linked from tutorials):**
- `realtime-channels.md` — **fixed this pass** (added `title`/`description`/`permalink`).
- `runtime-certification.md` — auto-generated cert; consider a small front-matter header.

## Navigation reachability

- New top-level pages added so every section is reachable: **About, Community,
  Enterprise, Contact, Blog**, plus a **404** page.
- Existing reachable sections: Home, Getting Started, Tutorials, Examples, Plugins,
  Deployment, Compare, FAQ, Roadmap, Changelog.
- **0 orphan navigation targets** and **0 broken internal links**
  (see [Broken Links Report](/broken-links-report/)).

## SEO

- `jekyll-seo-tag`, `jekyll-sitemap`, `jekyll-feed` enabled (`_config.yml`);
  canonical URLs, Open Graph, and Twitter cards emitted per page.
- **JSON-LD added:** `SoftwareApplication` schema on the Home page; `FAQPage`
  schema on the FAQ page; `FAQPage` already present on the five comparison pages.
- `robots.txt` and a generated `sitemap.xml` are served.

## Recommendations (ranked)

1. ✅ **Front-matter added to `realtime-channels.md`** (was linked from a tutorial without it).
2. ✅ **`SoftwareApplication` version refreshed** in `head_custom.html` (1.0.7 → 1.0.9); JSON-LD is centralized there (Software/FAQ/Breadcrumb/APIReference) — do **not** duplicate inline on pages.
3. ✅ **`favicon.svg` added** and wired in `head_custom.html`. **Still recommended:** generate raster `favicon.ico` + `apple-touch-icon.png` from `logo.svg` for older-browser/iOS coverage (binary assets — not generated here), then add their `<link>` tags.
4. Add a `docs-audit` CI job running `scripts/audit/site-audit.mjs` to keep broken-links at zero.
5. Add anchor-validation (`#heading` targets) to the audit script.

## Reproduce

```bash
node scripts/audit/site-audit.mjs
```
