---
layout: default
title: "Broken Links Report"
nav_exclude: true
search_exclude: true
permalink: /broken-links-report/
description: "Internal link + navigation audit of the StreetJS site."
---

# Broken Links Report

> Generated by `node scripts/audit/site-audit.mjs` (zero-dependency crawler over
> `docs/**`). Re-run any time; this report reflects the state at last run.

## Summary

| Metric | Value |
|--------|------:|
| Pages crawled | 142 |
| Known permalinks | 134 |
| **Broken internal links** | **0** |
| Pages missing `title` | 0 |
| Pages missing `description` | 0 |
| Files without front-matter | 12 (deep report docs — see [Documentation Audit](/documentation-audit/)) |

## What was checked

- Every markdown link (`](…)`) and HTML `href` attribute (including `site.baseurl`).
- A link resolves if it matches a page `permalink` **or** an existing file path
  (`docs/<path>.md|.html` or `docs/<path>/index.md`) — Jekyll serves both.
- Asset links (`.png/.svg/.css/.js/…`) are excluded from link resolution.

## Fixes applied this pass

| Was | Fix |
|-----|-----|
| `/community/` (no page) | Created `docs/community/index.md` (Community hub) |
| `/enterprise/` (no page) | Created `docs/enterprise/index.md` (Enterprise hub) |
| `/community/contributor-path.md` (`.md` URL) | Repointed to `/community/contributor-path/` |

## Methodology notes / limitations

- **Internal links only.** External URLs (github.com, npmjs.com, fonts) are not
  fetched here.
- **Anchors (`#…`) are not validated** against in-page headings yet — a future
  enhancement.
- Run alongside the GitHub Pages build (`pages.yml`), which is the source of truth
  for actual rendering.

## Reproduce

```bash
node scripts/audit/site-audit.mjs
```
