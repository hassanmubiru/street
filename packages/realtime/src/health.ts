// src/health.ts
// Realtime health check + metrics wiring (Req 17).
//
// Registers a realtime health check with `HealthCheckRegistry` (reported via
// `/health/*`) and exports connection-count and per-Room member-count metrics
// through `MetricsRegistry`. Implemented in task 12.1; this file reserves the
// module in the src/ layout.

export {};
